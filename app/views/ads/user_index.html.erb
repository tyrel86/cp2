<a data-toggle="modal" href="<%= @create_link %>" class="btn btn-glow btn-success new-btn">Create New Ad</a>
<div class="manage-index"> 
 <ul>  
    <% @ads.each do |ad| %>
      <li>
        <%= render "ads/ad", ad: ad  %>
      </li>
    <% end %>
  </ul>
</div>

<%= render "modals/create_ad" %>
