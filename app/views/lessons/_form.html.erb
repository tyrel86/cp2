<% content_id = lesson.id %>
<% content_id ||= "create" %>

<%= simple_form_for lesson do |f| %>
	<%= f.input :title, :label => 'Title' %>
	<%= f.input :body, :label => 'Content', input_html: {class: "tinymce", id: "lesson-content-#{content_id}"} %>
	<%= f.button :submit, class: "btn-success" %>
<% end %>
